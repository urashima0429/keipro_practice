#include <iostream>
#include <vector>
using namespace std;


int main(){
    int N, M, Q, A[16], a[64], b[64], c[64], d[64];

    cin >> N >> M >> Q;
    for (int i = 0; i < Q; ++i){
        cin >> a[i] >> b[i] >> c[i] >> d[i];
    }

    for (int i = 0; i < N; ++i){
        A[i] = 1;
    }

    for (int i = 0; i < N; ++i){
        
        for (int )
    }
    

    return 0;
}